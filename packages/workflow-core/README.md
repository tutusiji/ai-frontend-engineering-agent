# workflow-core

这个包负责工作流图、节点执行状态机、重试策略以及审批闸门的核心能力。

## 当前进度

- 已有工作流类型定义
- 已有 YAML loader
- 已有基础工作流校验器
- 已有最小串行执行器
- 已有本地 mock runner

## 后续职责

- 从 `workflows/*.yaml` 加载工作流定义
- 校验节点拓扑、依赖关系与 schema 引用
- 按依赖顺序执行 agent / plugin 节点
- 追踪运行状态和中间产物
- 在验证失败时触发 fix loop
- 在审批闸门暂停并等待恢复执行
