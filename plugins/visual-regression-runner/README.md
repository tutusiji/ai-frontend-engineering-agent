# visual-regression-runner

负责基于 target profile、生成产物线索和目标仓库现状，判断视觉回归验证是否可执行，并输出统一验证结果。

## 当前能力

- 根据 target profile 判断视觉回归是否支持
- 扫描视觉基线目录、截图产物目录和测试线索
- 输出 `runnerStatus`、基线线索和建议，不直接执行截图对比命令

当前版本为诊断型校验：优先回答“该不该做、现在能不能做、缺什么”，后续再补真实截图执行与 diff 产物。
